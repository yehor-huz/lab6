import { ShortTermProducts } from './short-term-products';

describe('ShortTermProducts', () => {
  it('should create an instance', () => {
    expect(new ShortTermProducts()).toBeTruthy();
  });
});
